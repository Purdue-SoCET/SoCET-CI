# Used to Build Image for ghrc

FROM alpine:latest

RUN apk add git python3 py3-pip openssh
RUN pip3 install fusesoc
RUN mkdir ~/.ssh
RUN ssh-keyscan github.com >>~/.ssh/known_hosts

# Copy Verilator Executables
COPY bin /bin